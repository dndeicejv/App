<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalVariable"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Hyper_Activity" />
        <activity android:name=".Lower_Activity" />
        <activity android:name=".User_Activity" />
        <activity android:name=".Search_Activity" />
        <activity android:name=".New_Activity" />
        <activity android:name=".Detail_Activity" />
        <activity android:name=".StringActivity" />
        <activity android:name=".NewoneActivity"></activity>
    </application>

</manifest>